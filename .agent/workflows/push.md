---
description: Sincroniza los cambios actuales con el repositorio de GitHub (commit y push).
---

Este flujo automatiza la subida de cambios.

1. Añadir todos los cambios
```powershell
git add .
```

2. Crear el commit con un mensaje descriptivo
```powershell
git commit -m "Actualización automática de Antigravity: Mejoras y correcciones"
```

// turbo
3. Subir los cambios a GitHub
```powershell
git push origin main
```

4. Confirmar al usuario que los cambios están en la nube.
